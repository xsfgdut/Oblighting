
先完成本地功能
完成服务器部分，由本地类库实现上传前收集，创建解释类库将本地数据转化成上传给服务器的版本，由此达到类设计的解耦合。

取节点过程中，只要是最后一帧数据，编号都是0xff,
如果obox内无节点则最后数据为空

数据部分

数据池：统一提供各页面数据，便于管理与使用，便于页面数据修改后源数据也做响应，存储方式采用map，
取出方式采用list


数据库

以选定obox序列号为表名称保持其唯一性
以obox序列号加房间主键id为名称创建房间内节数据表保存房间内节点和房间内情景信息


登陆页面的处理流程
在点击本地版本登录后才进行udp和获取obox名称的过程，进入页面后再进行节点情景数据收集；
添加服务器的操作在本地登陆进行，只需添加http交互
同时，服务器登陆的时候不进行任何本地连接操作；


数据收集部分：

1.获取obox版本信息
2.收集单节点id
3.收集单节点状态
4.收集组节点
5.组织节点组关系

6.获取情景id 如果id为空则表示没有情景，直接返回true表示情景接收结束,否则保存情景，next；
7.获取情景条件，情景条件的选项与获取条件id的选项重合，在此不对其做操作；唯独单一组单一子条件才可显示情景类型，否则均为未知类型情景；
8.获取情景行为节点，如果情景内节点地址是空则或者返回节点编号是ff则表示当前情景内节点接收结束，判断当前的情景编号，如果不是0xff则根
据记录编号值请求下一个情景id，否则整个方法返回true表示情景接收结束
由于本地设置的情景只包含本obox内部的节点，所以每取到一个情景内节点都可以从收集的数据中通过完整地址比对找到具体的实际节点；

在每一轮完整取数据结束后，要及时放置于数据池中，并申请新的数据引用；

组展示部分：
node有个参数是组地址，经过组织算法，存在于组中的节点此组地址为非0，可以此判断是否在组中；采用临时的列表展现数据，但是其内部包含的是实际的引用，
如此，可以简化拿取情景内节点的功能实现，以及通用于各项不同设备控制页面与情景节点类型选择不同设备类型的页面生成，
因为实际引用的保持，可以确保任何操作都是对实例操作，使数据同步

设置情景部分，0为不使能 1为使能  2为立即执行情景，不管有条件情景还是无条件情景


完成节点释放之后，等待6秒进行其他操作， flash擦出操作要用6秒的时间


task
完成位置的适配器，包括位置内节点适配器，位置内情景适配器，包括服务器版本和本地版本，暂时对本地版本进行空实现 ok
适配器对界面的应用 N
数据池添加位置和情景信息，数据池保持对目前操作对象的引用，便于数据同步 Y
控制灯页面实现 N